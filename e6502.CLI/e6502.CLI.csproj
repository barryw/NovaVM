<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
      <Folder Include="Resources\" />
    </ItemGroup>

    <ItemGroup>
      <ProjectReference Include="..\e6502\e6502.csproj" />
    </ItemGroup>

    <ItemGroup>
      <None Update="Resources\ehbasic.bin">
        <CopyToOutputDirectory>Always</CopyToOutputDirectory>
      </None>
      <None Update="Resources\extension.bin">
        <CopyToOutputDirectory>Always</CopyToOutputDirectory>
      </None>
    </ItemGroup>

    <!-- Set SkipRomBuild=true to compile without the cc65 toolchain (ca65/ld65).
         Mirrors the gate already honored by e6502.Avalonia so a solution build
         can succeed on machines that don't have the ROM build tools installed. -->
    <Target Name="AssembleRom" BeforeTargets="PreBuildEvent" Condition="'$(SkipRomBuild)' != 'true'">
      <Exec Command="make -C $(ProjectDir)../ehbasic" />
      <Copy SourceFiles="$(ProjectDir)../ehbasic/basic.bin" DestinationFiles="$(ProjectDir)Resources/ehbasic.bin" />
      <Copy SourceFiles="$(ProjectDir)../ehbasic/extension.bin" DestinationFiles="$(ProjectDir)Resources/extension.bin" />
    </Target>

</Project>
