NPP 2
MAIN MAIN.PAS
UNITPATH SYSTEM
UNITPATH USER
OUTPUT LIFE.BIN
OPTIMIZE O2
CONFIG INLINE
MAP LIFE.MAP
LABEL LIFE.LBL
MEMORY {
    RAM: start = $8000, size = $2000, file = %O;
}

SEGMENTS {
    CODE: load = RAM, type = ro;
    RODATA: load = RAM, type = ro;
    BSS: load = RAM, type = bss;
}
