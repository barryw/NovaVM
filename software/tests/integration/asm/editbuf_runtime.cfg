MEMORY {
    ZP:  file = "", start = $0080, size = $0080, type = rw;
    RAM: file = %O, start = $4000, size = $5000;
}

SEGMENTS {
    ZEROPAGE: load = ZP,  type = zp;
    CODE:     load = RAM, type = rw;
    RODATA:   load = RAM, type = rw;
    BSS:      load = RAM, type = bss, define = yes;
}
