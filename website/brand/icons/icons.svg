<!-- Walker Heavy Industries icon sprite — vendored subset for the NovaVM site.
     Source of truth: whi-brand web/brand/icons/ (built by web/build.sh into a
     full sprite). Only the `whi-download` glyph is vendored here because that is
     the only WHI icon the NovaVM site references (the shared download-latest
     control). Re-vendor from the source sprite if more controls are added. -->
<svg xmlns="http://www.w3.org/2000/svg" style="display:none" aria-hidden="true">
<symbol id="whi-download" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 4v11"/><path d="M8 11l4 4 4-4"/><path d="M5 19h14"/></symbol>
</svg>
