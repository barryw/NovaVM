# ld65 config for larger diagnostics loaded into RAM at $9000.
#
# $9000-$9FFF is available before the VGC register page at $A000.

MEMORY {
    CODE: start = $9000, size = $1000, file = %O;
}

SEGMENTS {
    CODE: load = CODE, type = ro;
}
