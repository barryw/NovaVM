---
title: "GET"
type: command
category: I/O
keywords: [get, i/o]
syntax: "GET var"
---

Read a single keypress into variable (non-blocking).

Variable names are case-insensitive, and only the first two characters are
significant. Prefer one- or two-character names such as `K` or `KY`.
