---
title: "VARPTR"
type: function
category: String
keywords: [varptr, string]
syntax: "VARPTR(var)"
---

Address of a numeric or string variable in memory.

Variable names are case-insensitive, and only the first two characters are
significant. `VARPTR(SC)` and `VARPTR(SCORE)` refer to the same numeric
variable.
