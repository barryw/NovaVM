# Zork Zero (V6) smoke — M3 Task 4 interim (picture_data live, no blits yet).
# Boot layout is pinned by --expect-at in project.mk. The turns below pin
# response text; positional .expect-at re-pins follow in Task 7 once
# draw_picture (Task 5) and the CR-interrupt margin release (Task 6) settle
# the final layout.
look=>Banquet Hall
inventory=>platter of hellhound bones
wait=>Time passes
